/**
 * PostCSS configuration for Tailwind CSS and Autoprefixer. Tailwind CSS
 * generates utility classes based on our Tailwind configuration, and
 * Autoprefixer adds vendor prefixes to CSS rules when needed.
 */
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
